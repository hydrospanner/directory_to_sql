dist: xenial
language: python
python:
  - 3.7
before_install:
  - sudo apt-get install sqlite
install:
  - python setup.py install
  - pip install coverage
services: sqlite3
script:
  - nosetests -v --with-coverage