dist: xenial
language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
  - 3.6
  - 3.7
install:
  - python setup.py install
  - pip install coverage
services: sqlite3
script:
  - nosetests -v --with-coverage